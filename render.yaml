services:
  - type: web
    name: shared-state
    env: python
    plan: free
    buildCommand: "pip install -r requirements.txt"
    startCommand: "uvicorn server_state:app --host 0.0.0.0 --port 10000"
